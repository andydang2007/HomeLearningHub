# Home Learning Hub - 产品设计文档

> 本文档是项目的**产品与业务设计事实来源**。它回答“这个产品应该是什么、核心体验如何运作、商业逻辑是什么”。工程实现、数据库、迁移、文件边界请看 `TECHNICAL.md`。

## 1. 产品定位

Home Learning Hub 是一个面向新加坡小学家庭的游戏化学习与家庭奖励系统。孩子每天通过短练习、专项小游戏和打卡获得成就感；家长通过后台管理孩子档案、题目/听写材料、奖励商品和兑换审批。

核心不是“无限刷题”，而是：

- 每天少量、高质量、可坚持的学习习惯。
- 用徽章、连击、金币、水晶、奖励游戏和未来盲盒提高粘性。
- 让多孩家庭可以形成互相提醒和鼓励，而不是互相攀比。
- 让家长掌控规则、奖励与边界。

### 1.1 游客模式与注册转化（已定方向）

产品允许**家长注册前**，孩子先在本机开始玩。这是期望路径，不是异常。

| 阶段 | 数据存在哪里 | 规则 |
|------|--------------|------|
| 注册前（游客） | 本机 `localStorage`，按**孩子名字** | 可玩、可攒徽章/金币/水晶/打卡/错题（本地键） |
| 注册后（家庭账户） | 云端 + 本机缓存，按 **`profile_id`** | 成就、资产、错题（云端用 `question_id`）长期保存 |

**注册前（接受的风险）：**

- 换设备、清空浏览器缓存 → 本地成就丢失，无法恢复。
- 孩子改名 → 旧名字下的本地数据对不上，视为丢失。
- 不追求跨设备同步。

**注册前（产品要做的）：**

- 本机学习数据（含徽章、金币、水晶、打卡、练习统计、本地错题）建议**最多保留 30 天**；临近到期在首页提醒：“找家长注册，保存你的徽章和奖励”。
- 注册不是清空重来，而是**首次绑定 + 导入**：把本机各名字下的数据一次性迁到对应云端孩子档案（家长确认匹配或自动按名字匹配）。

**注册后（承诺）：**

- 绑定到家庭账户后，**所有资产与学习状态必须上云**；云端是唯一权威，本机仅作缓存展示。
- 换一台全新设备、清空浏览器后，用同一家庭账号登录，体验应与旧设备**完全一致**（徽章、金币、个人水晶、打卡、连击、每日练习完成状态、待审批兑换等）。
- 当前实现里「每日每科只能练一次」若只写在本机 `localStorage`，换机可绕过——这属于缺陷；注册后必须改为**云端判定**（例如按 `profile_id + 科目 + 新加坡日期` 记录是否已完成）。
- 云端错题本**必须**使用 `question_id`；本地练习若没有错题，组卷仍按 A+B+C 自动填满 15 题（C 题补足）。

**注册后必须上云的数据（清单）：**

| 类别 | 示例 |
|------|------|
| 资产余额 | 金币、个人水晶 |
| 资产流水 | 练习奖励、兑换扣款、退款；手动调整暂不开放，后期如开放也必须记流水 |
| 徽章 | 科目/技能/隐藏/连击计数与获得事件 |
| 练习与限制 | 每场 `learning_sessions`、是否已完成今日该科练习 |
| 错题 | `student_mistakes_book` + 错误答案历史 |
| 激励附属 | 奖励游戏 ticket、待家长审批的兑换单 |
| 打卡连击 | 累计天数、当前连击、历史最高连击 |

**转化话术：** “你已经攒了 X 个徽章 / Y 天打卡，让家长注册就能永久保存。”

### 1.2 界面语言策略（已定）

- 文案仍集中在 `common/js/i18n.js`（`en` / `zh` 两套词典），但**不做“改一屏补一屏 key”**的增量流程。
- **一次性设置语言偏好**（存云端，注册后随 profile 同步）：
  - **孩子界面语言**：每个孩子一条，存在孩子 `profiles`（如 `ui_lang`：`en` | `zh`）。家长后台为孩子选择；孩子端按当前 active 孩子档案的语言渲染。
  - **家长界面语言**：家庭/家长账号级全局设置（如 `parent_ui_lang`），家长登录后所有 `parent/` 页面使用该语言。
- **开发默认**：新 UI 先用英文设计与实现；通过**专项 i18n 轮次**一次性补全 `zh` 词典，而不是每个功能 PR 都强制双语。
- 旧屏在未汉化前可继续以英文或已有文案显示；语言切换只改设置，不要求每屏单独维护“语言 key 清单”。

### 1.3 云端同步体验（Google Sheet → Supabase）

**过去（Google Sheet）：**

- 本地缓存与云端 Sheet 容易不一致，曾出现“云端已更新、本机还显示旧数”。
- 缓解方式：每次打开强制拉取 + 手动「同步」按钮。

**目标（Supabase，注册后家庭）：**

- **云端为唯一权威**；本机不保存“最终余额”或“今日是否已练”的真相副本。
- 打开学生端 / 切换孩子 / 进入家长后台时：**自动拉取**该家庭必要摘要（余额、徽章计数、今日完成、待审批兑换等）。
- 练习结束、兑换、发徽章等：**RPC 写库成功后再更新界面**，不先改 localStorage 再等同步。
- 可选保留「刷新」按钮作心理安全网，但正常路径应**无感**；不必再依赖 Sheet 式全量对账。
- 若使用短时缓存，必须带 `updated_at` 或版本号；写操作成功后**作废**相关缓存，避免重复出现旧 Google Sheet 时代的滞后问题。

## 2. 首页体验：先看徽章墙

孩子打开页面后，第一眼应该看到自己的徽章墙和学习状态。徽章墙不是装饰，而是孩子每天回来的主要动机。

首页核心信息：

- 当前孩子头像、姓名、年级。
- 今日是否已打卡 / 连击状态。
- 徽章墙，分成四个区域。
- 科目入口：English、Math、Science、华文。
- 神秘商店入口。
- 切换孩子入口。
- 家长入口。

## 3. 徽章系统

### 3.1 科目成就徽章

科目成就来自完成对应练习或专项练习。

当前应有 7 个：


| 徽章               | 来源     |
| ---------------- | ------ |
| English Star     | 英文基础练习 |
| Math Genius      | 数学基础练习 |
| Science Pro      | 科学基础练习 |
| Chinese Ace      | 华文基础练习 |
| Pinyin Pro       | 华文拼音游戏 |
| Dictation King   | 华文听写   |
| Character Spirit | 华文部件游戏 |


规则：

- 每个科目/专项每天最多获得 1 个对应科目徽章。
- 基础练习完成后，如果达到该科基础正确率，才应发放科目徽章和奖励游戏 ticket。
- **1 个徽章 = 1 个金币**。孩子每获得 1 枚徽章，系统同步给该孩子档案记 1 个金币，用于兑换实物奖励。
- 打卡水晶不是“打开 App 即得”；孩子当天至少获得 1 枚徽章后，才可解锁当天打卡水晶。
- 科目徽章可以与技能徽章、隐藏惊喜徽章同日叠加。

### 3.2 技能成就徽章

技能徽章奖励“做得好”而不只是“做完”。

当前设计：


| 徽章                   | 条件                    | 家长后台是否可配置 |
| -------------------- | --------------------- | --------- |
| Sharpshooter         | 单次基础练习达到家长设置的目标正确率 | 是         |
| Speed Record / 极速突破  | 单次练习在家长设置的目标完成时间内完成，且达到目标正确率 | 是         |
| Unlock Game / 解锁奖励游戏 | 获得奖励游戏资格或首次进入奖励游戏系统   | 可讨论       |


说明：

- 原“气球猎人”徽章是旧版只有一个游戏时遗留，不应绑定某一个奖励游戏。
- 建议改为“解锁游戏”或“Game Pass”类徽章，表示孩子通过学习打开了奖励游戏功能。
- `catcher` 暂时不单独设计徽章，避免每新增一个解压游戏都要补一个徽章体系。
- 家长后台应为每个科目设置：**基础正确率**（获得科目徽章）、**目标正确率**（获得额外技能徽章）、**目标完成时间**（获得速度类额外徽章）。

### 3.3 隐藏惊喜徽章

隐藏惊喜徽章没有获得前不显示占位，获得后才出现在徽章墙。


| 徽章                    | 条件                        |
| --------------------- | ------------------------- |
| Early Bird / 早起鸟儿     | 7:00 前完成一次达到基础正确率的练习      |
| Night Owl / 小猫头鹰      | 22:00 后完成一次达到基础正确率的练习     |
| Hat-trick / 全对三连      | 连续 3 次练习全对                |
| Weekend Maniac / 周末狂人 | 单个周末获得 15 个以上徽章           |
| Holiday Charge / 假期充电 | 新加坡本地小学假期期间，累计打卡 15 天后触发 |


说明：

- 隐藏徽章可以和科目/技能徽章同一天叠加。
- 高频叠加时一天可能获得很多徽章，需要后续设计“徽章消耗/合成/盲盒”机制，避免徽章只堆积不流动。

### 3.4 连击徽章

连击区展示坚持感，和普通徽章区分开。

规则：

- 最前面显示“历史最高连击记录”。
- 连击达到 3、5、10、15、30 天时陆续点亮徽章。
- 点亮后，不因为继续连击而熄灭前面的徽章。
- 超过 30 天后，每增加 10 天给一枚新的连击徽章。
- 展示窗口向前滚动：例如达到 40 天后，墙上展示 `5, 10, 15, 30, 40`，最短的 `3` 移出当前墙。
- 如果中断连击，则当前连击徽章全部重新熄灭并重新开始。
- 历史最高记录保留展示，直到被新的最高连击打破。

## 4. 学习与练习系统

### 4.1 每日限制

所有正式练习每天限做一次。

需要修正的产品规则：

- 不应在“点进去”时就算完成。
- 必须完成最后一题并提交整场练习后，才计入今日已完成。
- 如果孩子中途退出，不应锁死当天入口。
- 每个科目徽章每天最多拿一次，但可以叠加技能徽章和隐藏徽章。

### 4.2 基础练习：A + B + C = 15

每次标准练习固定 15 题。


| 类型     | 数量                      | 来源        |
| ------ | ----------------------- | --------- |
| A 错题   | 错题数 > 30 时抽 5 题，否则抽 3 题 | 错题本，按权重加权 |
| B 降维复习 | 固定 2 题                  | 低一个年级     |
| C 当前题  | 补足到 15 题                | 当前年级题库    |


错题权重：

- 初次答错加入错题本。
- 一个 session 里同一道题只增加一次权重，避免孩子连续乱点提交导致权重异常。
- 如果孩子做错后系统提示正确答案，但孩子没有真正改对又点提交，不应再次增加错题权重。
- 答对后权重下降，降到 0 后回到普通题池。
- 错题本不作为孩子端独立页面展示；孩子只是在练习中抽到错题并把它做对。系统必须保留错误答案历史，方便家长在学习报告里看到典型错误和孩子曾经填写的错误答案。

### 4.2.1 学期、周次与出题范围

首页需要展示当前学期和周次，例如 `Term 2 Week 4`。

出题不能只按年级粗暴拉取全部题目。例如 P3 一开学时，不能把整个 P3 全年题库都放进 C 题池。

范围控制原则：

- 系统可以根据新加坡本地小学学期日历展示当前 `Term X Week Y`。
- 题库应按 `subject + grade + term + topic/lesson` 组织。
- 孩子或家长需要能选择“当前学到哪里”。
- 华文优先按“第几课 / lesson”控制范围。
- 英文、数学、科学优先按 topic 控制范围。
- A 错题可以来自已学范围内的错题。
- B 降维复习来自低一年级已学/可复习范围。
- C 新题默认从家长点选的当前学习范围抽，不应跨到未学 topic/chapter/lesson。

出题逻辑后续需要单独细化，包括 topic 权重、临近期末复习、假期复习和家长手动覆盖。

#### 4.2.1.1 各科「范围模式」开关（每孩独立）

所有科目都可由家长为每个孩子单独选择两种出题范围模式：

| 模式 | 含义 | 适合谁 |
|------|------|--------|
| **`selected_scope`（默认，家长点选范围）** | C 题只从家长为该孩子点选的 topic / chapter / lesson 中抽 | 跟学校进度走的常规家庭；避免出未学题打击信心 |
| **`full_grade`（开放整年级）** | C 题从该孩子**当前年级**全部 active 题库抽，不再受 `profile_learning_scope` 限制 | 程度好、走在学校前面、想要更大练习空间的孩子 |

规则细节：

- 对 English、Chinese、Math、Science 全科生效。
- A 错题、B 降维复习的抽取规则**不受**此开关影响。
- 字段挂在每孩每科设置上，由家长后台分别设置；多孩家庭可一个孩子按家长点选范围，另一个孩子开放整年级题库。
- 切换时立即生效；不回填历史 session。

### 4.2.2 家长上传错题与版权边界

- 家长可通过 OCR / AI 上传错题或听写材料（经家长确认后入库）。
- **隐私与侵权防护：** 家长上传的错题、自定义题目、听写条目，**仅绑定并展示在该上传所对应的孩子 `profile_id` 下**；其他孩子 Profile、其他家庭、公共题库池均不可见。
- 不得将某孩子 Profile 下的家长上传内容自动合并进全站共享 `questions` 表供所有人练习，除非家长明确授权且完成合规审核（默认关闭）。
- 技术层通过 RLS：`parent_imported_questions.profile_id = 当前孩子` 强制隔离。

### 4.3 英文

英文模块包含：

- 基础练习。
- Spelling。
- Wordsearch 小游戏。

Spelling 目标：

- **系统自带默认听写表**（按年级 + Term + Week 组织，所有家庭可读不可改），家长**不上传也能用**。
- 家长可拍照 OCR 上传**自家学校**的听写表，作为对默认表的补充或替换（仅本孩可见，见 §4.2.2）。
- 当前发音引擎效果不佳，听不清，需要重做。
- 重做前不应把它作为核心可靠功能。

Wordsearch：

- 难度较低。
- 不限次数。
- 不给徽章。
- 作为轻量休闲学习入口。

专项页面清单：

- `spelling`：英文听写，未来支持 AI OCR 上传学校听写表。
- `wordsearch`：英文词搜索小游戏，不发徽章，不限次数。

### 4.4 数学与科学

当前只有基础练习题。

后续可以扩展专项游戏，但当前核心是题库质量、错题复习和每日完成体验。

### 4.5 华文

华文模块包含：

- 华文基础练习。
- 听写。
- 拼音游戏。
- 部件游戏。

华文听写：

- **系统自带默认听写表**（按年级 + Term + Week + Lesson 组织），家长**不上传也能用**。
- 家长可上传**自家学校**的华文听写表（OCR 后家长确认入库），作为对默认表的补充或替换（仅本孩可见，见 §4.2.2）。
- 因发音引擎问题，当前更适合"看拼音，在米字格手写汉字"。
- 分为练习模式和挑战模式。
- 练习模式每次一课。
- 不会时可以长按偷看，松手后消失。
- 如果内容太简单，孩子应可跳过。比如高年级可能出现“大彻大悟”这类词语，不应强迫孩子反复写单个“大”字，避免厌烦。
- 挑战模式暂留坑，后续单独设计。

拼音游戏与部件游戏：

- 属于华文专项练习。
- 各自每天最多给 1 个专项徽章。

专项页面清单：

- `tingxie`：华文听写。
- `pinyinshooter`：拼音专项游戏。
- `radical`：部件/汉字结构专项游戏。

### 4.6 图书馆

图书馆功能存在，但当前比较简陋，主要问题是书少。

定位：

- 先保留入口。
- 后续补英文与华文书目。
- 不作为当前 MVP 的关键闭环。

## 5. 奖励游戏系统

奖励游戏是纯解压，不承担学习判分。

当前逻辑：

- 完成 1 次达到基础正确率的正式练习，发 1 张 ticket。
- 2 张 ticket 可玩 1 次奖励游戏。
- 奖励游戏没有公开入口，只能由后台/系统跳转进入。
- 结束后自动跳回练习主页。
- 进入时必须检查 ticket。
- 直接输入网址访问，应被踢回主页。

奖励游戏后台是一个分发台：

- 随机分配一个已写好的奖励游戏。
- 当前有 `popballoon` 和 `catcher`。
- 后续可继续新增游戏。
- 新增游戏不必强制新增专属徽章。
- 代码上应有单独 `game/` 区域，放游戏分发台和小游戏页面。

## 6. 神秘商店与家庭经济

### 6.1 两类资产


| 资产  | 归属     | 主要用途                |
| --- | ------ | ------------------- |
| 金币  | 孩子档案名下 | 兑换实物奖励，如快餐、零食、文具、玩具等；每获得 1 枚徽章同步获得 1 个金币 |
| 水晶  | 孩子个人资产 | 兑换非实物奖励，如电影之夜、周末露营、免做家务、游戏时间等；当天至少获得 1 枚徽章后解锁打卡水晶 |


显示规则：

- 金币和水晶都显示在当前孩子档案下。
- 多孩家庭中，孩子身份 PIN 用于防止大孩进入小孩档案刷低年级题、刷金币骗奖励或占用小孩当天练习机会。

### 6.2 商品类型

当前：

- 金币兑换实物奖励，如快餐、零食、文具、玩具等。
- 水晶兑换非实物奖励，如电影之夜、周末露营、免做家务、游戏时间等。

未来商品可分为：

- 实物奖励：玩具、书籍、快餐、零食、文具等，使用金币。
- 非实物奖励：电影之夜、周末露营、免做家务、游戏时间、室内游乐场、staycation、旅行等，使用个人水晶。

### 6.3 家长后台配置

家长应能：

- 从模板添加奖励商品。
- 自定义奖励商品。
- 设置库存、价格、是否启用。
- 查看孩子兑换申请。
- 批准或拒绝兑换。

系统应给家长价格指引：

- 用“价值多少天练习/打卡”表达价格，而不只是裸数字。
- 例如：小奖励约 3-5 天；中等奖励约 1-2 周；高价值奖励约 1 个月或更久。

### 6.4 兑换与背包

当前兑换码可以移除：

- 孩子兑换后，不需要显示兑换码。
- 家长后台直接显示兑换申请。

当前背包的退款功能也可以移除：

- 退款/撤销应放到家长后台。
- 避免孩子误操作后自己反复退款。

## 7. 徽章等级、段位与消耗

### 7.1 问题与目标

- 孩子每天可获得多枚徽章（科目、技能、隐藏等）；若只增不耗，徽章墙会通胀、页面杂乱。
- 用「等级 + 段位」提供长期目标：**升级时消耗的就是徽章墙上原有的那些徽章**（同一套数据，不另建“可消耗池”）。
- 消耗后对应徽章数量减少（或从墙上移除/折叠），**核心目的：抑制徽章通胀、让主页更清爽**。
- 逢五/逢十里程碑另需消耗**水晶**（与徽章消耗并行，见 §7.4–7.5）。

### 7.2 等级与段位

- **等级（Level）：** 界面以递增数字显示（Level 1、2、3…）。
- **段位（Tier）：** 按等级区间映射五档，建议命名：

| 段位 | 建议等级区间（可调） |
|------|----------------------|
| 青铜 Bronze | Level 1–9 |
| 白银 Silver | Level 10–19 |
| 黄金 Gold | Level 20–29 |
| 铂金 Platinum | Level 30–39 |
| 传说 Legend | Level 40+ |

具体区间可在 `badge_level_config` 中配置，上线前用数值模拟平衡。

### 7.3 常规升级（消耗墙上徽章）

- 每次升级按配置表扣减**指定类型或总数的墙上徽章**（与 §3 科目/技能/隐藏等徽章一致，例如消耗 2 枚「English Star」或等价规则——具体公式在 `badge_level_config` 配置）。
- 扣减后 `profile_badge_counters` 同步减少；孩子端徽章墙立即反映，避免“墙越堆越长”。
- 升级所需数量随 Level 递增；由 RPC 执行，防止前端伪造扣减。

### 7.4 关卡升级（逢 5 / 逢 10）

- 升至 **Level 5、10、15、20…** 等整十、逢五里程碑时，除消耗常规徽章外，需**额外消耗固定数量水晶**（按里程碑配置，如 9→10 需 +N 水晶）。
- 水晶消耗在 RPC 中一次性扣减并记流水。

### 7.5 水晶产出与用途互斥（产品原则）

- **水晶每日打卡限产 1 颗**（每个孩子每自然日最多获得 1 颗打卡水晶，新加坡时区）。
- 系统**不替用户分配**水晶用途；由家庭自行在以下用途二选一或自行权衡：
  - **升级等级**（逢五/逢十里程碑或未来扩展的等级功能）
  - **家庭商城兑换**（个人水晶换奖励）
- 不在 UI 层强制引导“必须先升级再兑换”，仅展示余额与说明。

### 7.6 盲盒（远期，与等级并行）

- 保留类似盲盒/抽卡的粘性方向；抽卡消耗物（徽章/水晶/组合）、稀有度、未成年人合规等**单独评审**，不与等级表混在同一张配置里。

待设计：

- 每级扣减哪些徽章类型、各需几枚（数值平衡）。
- 里程碑水晶表。
- 等级与名人堂段位的关系（见 §10）。

## 8. 家长后台核心功能

家长后台最终应覆盖：

- 注册、登录、忘记密码。
- PIN 门。
- 添加/编辑/删除孩子档案。
- 设置孩子年级、学校、性别、头像、华文/高级华文、**孩子界面语言**（`ui_lang`）。
- 为孩子分别设置各科出题范围模式（家长点选 topic/chapter/lesson / 开放整年级题库，见 §4.2.1.1）。
- 为孩子开关/设置/重置 **3 位身份 PIN**（防同家庭刷号；见 §8.1）。
- 设置**家长界面语言**（家庭全局）。
- 为每个科目设置基础正确率（获得科目徽章）、目标正确率（获得额外徽章）和目标完成时间（速度类额外徽章）。
- 上传学校听写表或错题材料，支持 OCR；**上传内容仅归属当前操作所选孩子 Profile**（见 §4.2.2）。
- 管理奖励商品模板和自定义商品。
- 审批兑换；查看**兑换记录**（孩子点了什么、何时、消耗多少、状态：待批/已批/已退）。
- **不**向家长展示完整金币/水晶流水（每笔 +1、每笔徽章不必逐条列出）。
- 查看每个孩子**学习报告**（默认按周）：完成练习次数、新获徽章数、期间获得的金币/水晶**汇总**、学习时长、消灭错题数、典型错题与孩子填写过的错误答案等。
- 学习报告支持家长一键保存为 PDF；系统内报告缓存只保留最近一周，避免后台长期堆积报告文件。
- 当前余额：家长可在孩子端界面查看，或报告页显示**期末余额快照**，无需流水明细。
- 管理奖励游戏 ticket 和游戏访问权限。
- 设置当前学习进度：华文第几课，英文/数学/科学已学 topic。
- 查看孩子练习核心数据，例如学习时长、完成练习数、正确率、消灭错题数、错题变化、连续打卡与徽章获得。
- 也可以考虑用“每周报告”的形式呈现核心数据，而不是在 dashboard 堆太多数字。
- 提供「反馈与建议」入口，方便家长提交报错、题目问题、功能建议或奖励系统建议。
- 注册后首次同步本机数据时，可以展示一次“孩子历史学习摘要”，但具体表现待设计。
- **手动调整资产暂不开放：** MVP 阶段不提供家长手动加/扣金币或水晶，避免资产规则过早复杂化；后期如确有必要，再升级为带备注、带流水、仅家长可用的高级功能。
- **平台外任务（慎重边界）：** 可引导家长把“做家务、早睡、整理房间”等设为**非实物奖励**或家庭约定，而不是把平台做成育儿打卡 App。系统核心仍是**学习达成 → 资产 → 家长审批兑换**，避免与学校学习数据混为一谈，也避免过度收集生活隐私。

### 8.1 孩子身份 PIN（防同家庭降维刷号）

多孩家庭里，大孩可能用小孩账号做更低年级题，形成“降维打击”：短时间在小孩档案名下刷出大量徽章和金币，用来骗取实物奖励；同时也会把小孩当天可做的正式练习消耗掉，导致真正的小孩没有练习可做，学习数据也被扭曲。

**产品规则：**

- 家长后台可**逐个孩子**开启「身份 PIN」（默认关闭，单孩家庭一般无需开启）。
- PIN 为 **3 位数字**，定位是「软门」，威胁模型仅为家庭内部同住者，不是黑客。
- **设置/修改/重置**只能在家长后台（先过家长 4 位 PIN）。**孩子端不能改自己的 PIN**。
- **触发点**：学生 hub 选择某孩子卡片时，若其 PIN 已启用 → 弹窗输入 3 位 PIN → 验证通过才进入该孩子学习态。
- **重置**：孩子忘记 PIN，由家长 PIN 重置；**不走邮件**。

**安全与节流（与家长 PIN 同一套，复用而不另起一套）：**

- PIN 在云端以 **SHA-256 哈希**存储（与家长 PIN 思路一致），明文不进数据库。
- 客户端 5 次错误 → **30 秒指数退避**锁定；服务端 RPC 校验也加节流，避免本地绕过。
- PIN 只解锁**身份切换**，不等于家长权限：不能用孩子 PIN 进入家长后台、不能用它批准兑换。

**存储归属（与「注册后全部上云」原则一致）：**

| 阶段 | 存哪 | 备注 |
|------|------|------|
| 注册前（游客） | 本机 `kid_profiles` 元数据 | 跟其他游客数据一样换机会丢，可接受 |
| 注册后 | 云端 `profiles.kid_pin_hash` + `kid_pin_enabled`；本机仅缓存「是否需要 PIN」 | 换设备登录后规则保持一致 |

如果只把孩子 PIN 留在本地，会出现「换机器就没 PIN，刷号反而绕过」的漏洞，因此**注册后必须上云**。

### 8.2 账本、兑换记录与家长：防作弊不是“给孩子看”

- **后台仍保留完整 `wallet_ledger`**（每笔增减、练习奖励、兑换扣款、退款），用于系统对账与防双花；**家长界面默认不展示**。
- 家长侧财务相关只看 **兑换记录**（`reward_redemptions`）：申请、审批、拒绝并退款；足够处理“孩子换了什么、花多少”。
- 练习奖励、徽章、打卡带来的资产变化，放在**学习报告汇总**里（例如“本周完成 12 次练习，获得 8 枚徽章，金币 +8”），不必复刻旧版逐条 `coin_update` / `badge` Log。
- 孩子端可看当前金币与个人水晶总数；细节成就以徽章墙为主。
- 前端严格 + 敏感操作走 RPC，可以挡住大部分**直接改余额、重复兑换、伪造 ticket** 的技术作弊。
- 孩子仍可能“乱答题、刷本地”等；真正价值资产（兑换实物/特权）**必须经过家长审批**，因此经济闭环的最终闸门在家长，不在算法 alone。
- 若发现异常，家长可查看近期练习记录与兑换记录；MVP 阶段暂不提供手动调账，必要时由后续高级功能处理。

## 9. 订阅分层与家长分享裂变

### 9.0 账户类别与版本（总原则）

注册账户先按家庭孩子数量分为两类：

| 家庭类型 | 孩子数量 | 价格定位 |
|----------|----------|----------|
| **一孩账户** | 只能添加 1 个孩子 | 价格较低 |
| **多孩账户** | 最多添加 3 个孩子 | 价格较高 |

每个家庭类型下再分为基础版与高级版：

| 版本 | 功能范围 | 说明 |
|------|----------|------|
| **基础版（Basic）** | 与高级版功能相同 | 可长期使用；不因版本不同减少功能模块 |
| **高级版（Premium）** | 与基础版功能相同 | 通过试用、付费或赠送时长获得 |

关键原则：

- **一孩 / 多孩** 的唯一核心差别是可添加孩子数量不同，因此价格不同。
- **基础版 / 高级版** 的功能范围相同，不因版本不同隐藏学习、奖励、报告等核心功能。
- 高级版到期后回到基础版，不锁死账号与已有学习记录。
- 付费订阅购买的是对应家庭类型的高级版 entitlement（月付/年付），与 §9.1–9.5 赠送时长叠加规则在实现时写清。

### 9.1 新用户直接注册（无邀请码）

- 默认获得 **30 天高级版**（与付费高级版功能一致）。
- 30 天结束后 → **永久使用基础版**，可择机订阅高级版。

### 9.2 邀请注册（30 + 14，均为高级版）

- **老用户（邀请人）：** 家长后台生成**专属邀请链接**（绑定家庭 / 邀请码）。
- **新用户（被邀请人）：** 通过链接注册后获得的全是**高级版功能**，不是“基础试用”：
  - **立刻生效：30 天高级版**；
  - **待解锁：14 天高级版**（注册时显示为锁定，满足激活条件后并入到期日）。

即邀请路径最高可得 **30 + 14 = 44 天** 高级版（在达上限前），与直接注册的 30 天相比多 14 天激励。

### 9.3 激活条件（解锁 14 天）

- 被邀请家庭注册后，其**任一子账户（kid profile）** 需 **连续 3 天** 完成学习打卡（与产品内「完成达到基础正确率的正式练习并记入打卡」一致，不可仅打开页面）。

### 9.4 奖励发放

- **激活达成后：**
  - **新用户：** 解锁并到账 **14 天高级版**（与已有 30 天高级合并计算到期日）。
  - **老用户（邀请人）：** 高级版到期日**自动顺延 7 天**（高级功能，无需手动领取）。

### 9.5 上限与防刷

- 单个账户通过邀请渠道累计获得的**额外高级版赠送时长上限为 90 天**（指邀请奖励累加部分，如多次邀请所得的 7 天、被邀请方解锁的 14 天等；**首赠 30 天**是否计入上限可在实现时单独配置，默认建议：仅“邀请链产生的额外块”计入 90 天上限）。
- 需记录：`referrals` 表、激活状态、entitlement 到期日、设备/家庭风控（见 `TECHNICAL.md`）。

### 9.6 与订阅定价的关系

- 裂变与首赠只延长**高级版到期日**；不改变标价。
- 付费订阅与赠送时长叠加：建议付费周期与赠送取**较晚到期日**或排队规则，上线前定一条清晰家长说明。

---

## 10. 匿名名人堂机制

### 10.1 榜单分类

- **同年级榜单：** 仅展示与当前孩子相同年级（如 P3）的用户。
- **全新加坡总榜：** 跨年级全国（新加坡）聚合榜（仍受年级脱敏与段位分组规则约束）。

### 10.2 信息脱敏

- **不展示**用户所在学校名称。
- 用户展示名采用 **ID 打码**（如 `V***rie`），不使用完整真实姓名或邮箱。
- 注册时家长录入的真实姓名仅用于家庭内部，不进入公开榜。

### 10.3 段位展示（非绝对积分）

- 榜单**不展示绝对积分数字**，避免精确攀比与刷分动机。
- 列表按 **段位（青铜→传说）** 分组排列；组内可按内部排序键（如等级或近 30 天活跃徽章数）但不暴露具体分值给孩子端其他用户。

### 10.4 底部悬浮定位（本人锚点）

名人堂页面底部**固定悬浮**展示登录用户本人：

- 当前**段位**；
- 在**同年级 cohort** 内的**精确排名**（如 “P3 中第 12 名”）；
- 距离**下一等级**还需消耗的**墙上徽章数量**（与 §7.3 同一套徽章存量一致）。

他人只见脱敏 ID 与段位；本人见精确名次便于自我激励。

### 10.5 隐私与合规

- 名人堂为**可选功能**；家庭可在家长设置中关闭孩子参与排行（默认参与与否待产品决定）。
- 仅使用已脱敏字段；遵守 PDPA，不展示 LBS、学校、未满 14 岁可识别信息组合。

---

## 11. 商业计划与产品边界

商业方向：

- 家庭订阅制，适合 1-3 个孩子。
- 新加坡本地小学家庭作为第一目标用户。
- 先做家庭内部闭环，再做同校社交与名人堂（§10）。

建议定价方向：

- 一孩账户：月付暂定 S$12.90，只能添加 1 个孩子。
- 多孩账户：月付暂定 S$19.90，最多添加 3 个孩子。
- 年付：暂定 S$129 - S$199，根据一孩/多孩区分。
- 基础版与高级版功能范围相同；差别不在功能开关，而在账户家庭类型（一孩/多孩）与价格。
- 高级版通过 30 天首赠、邀请奖励、订阅获得（§9）。

隐私与社交边界：

- 不采集未成年人实时地理位置。
- 学校、年级、班级/小队应由家长管理。
- 未来同校/同年级社交需要严格 RLS，避免跨校陌生人可见。

品牌方向：

- BrainDash 可作为商业品牌候选。
- Home Learning Hub 可作为项目内部名称。
- 旧项目代号不再用于文档、UI、变量命名或新代码。

## 12. 当前明确的产品问题

必须修：

- 每日限制不能在进入练习时触发，必须在完成最后一题后触发。
- 中途退出练习不应锁死当天入口。
- 错题同一 session 内不应重复加权。
- 奖励游戏入口必须继续隐藏并检查 ticket。
- 气球猎人徽章应改名或重设为“解锁游戏”类徽章。

待设计：

- 华文听写挑战模式。
- 徽章等级每级消耗数量与类型（墙上徽章扣减公式）。
- 裂变邀请与 entitlement 叠加 Stripe 规则。
- 名人堂排序键与年级 cohort 计算。
- 盲盒/抽卡系统（§7.6）。
- 英文与华文 OCR 听写表上传的完整流程。
- 奖励商品价格指引公式。

