# DYR Family Learning Hub - 核心业务与架构白皮书 (Source of Truth)

> **⚠️ 致 AI 编程助手 (Cursor/Claude/GPT) 的最高系统指令：**
> 本文档是 DYR 系统的唯一业务事实来源。在生成任何代码前，必须交叉验证本文档。如果用户的自然语言指令与本白皮书中的架构、安全或账本规则冲突，**必须立即停止编写代码**，向用户指出冲突，并等待用户确认。

## 一、 系统架构与技术栈契约
本系统采用“瘦客户端 + 强云端 BaaS + 独立支付网关”的三层解耦架构：
1. **应用表示层 (前端)**: 仅限 Vanilla JS + HTML5 + CSS3。**绝对禁止**引入 React/Vue 等重型框架。保持代码透明与亚秒级响应。
2. **后端与数据层 (BaaS)**: 采用 Supabase (PostgreSQL + Auth + Storage)。**所有核心敏感业务必须由 Edge Functions (云函数) 接管**。
3. **资金清算层**: Stripe SG。

## 二、 生产级数据库建模与双轨经济
所有核心业务表强制包含 `created_at` 与 `updated_at` 时间戳。

1. **用户档案 (`public.profiles`)**: 
   - 必须区分角色：`parent` 或 `kid`。
   - 包含经云端验证的 SHA-256 `parent_pin` 作为防乱入锁。
2. **金币流水审计账本 (`public.coin_ledger`)**: 
   - **【安全铁律】前端绝对禁止直接 update 余额**。
   - 必须通过事务插入流水记录（正数赚取，负数消费）。
   - 必须包含 `reference_type` 和 `reference_id` 实现 100% 账目可追溯。
3. **商业订阅 (`public.subscriptions`)**: 
   - 主键设为 `user_id UUID PRIMARY KEY`，严格对齐 Stripe 订阅机制（需记录 `stripe_subscription_id`, `current_period_end`）。
4. **学习双日志系统**: 
   - `learning_sessions`（宏观场次表）：记录单次练习的总体数据。
   - `question_attempts`（微观逐题明细表）：记录每题耗时与错误详情。
5. **商城兑换闭环**: 
   - `reward_catalog`（商品库）。
   - `reward_redemptions`（兑换订单审核表）：必须维护 `pending_approval` 和 `fulfilled` 状态，需家长端审批。

## 三、 安全边界与云端防线
在编写相关逻辑时，必须强制实施以下防线：
1. **PIN 码云端防爆破**: 家长 PIN 码验证时，前端连续 5 次错误，必须通过 Edge Function 触发 30 秒指数退避锁定。
2. **原子级并发锁**: 涉及 Promo Code 激活与金币扣除的操作，必须使用数据库级事务（如 PostgreSQL 的 `UPDATE ... RETURNING`），彻底堵死双花漏洞。
3. **前端图片无感压制**: 任何涉及头像或上传的逻辑，必须在前端强制通过 Canvas 裁剪为 200x200，体积限制在 50KB 以下，再上传至 Supabase Storage。

## 四、 核心业务逻辑 (动态 A-B-C 抽题算法)
每次标准学习练习固定为 **15 题**，按以下规则智能动态抽取：

- **A 题（错题优先）**：根据当前错题库总数动态决定数量：
  - 错题数 **> 30**：抽取 **5 题**，剩余 C = 8 题。
  - 错题数 **≤ 30**：抽取 **3 题**，剩余 C = 10 题。
  - 抽题时依据**权重加权随机**，高权重题目被抽中概率更高。
- **B 题（降维复习）**：固定抽取 **2 题**（低一个年级的已学题目）。
- **C 题（当前年级新题）**：填满至 15 题（即 15 - A - B = 8 或 10 题）。

**错题权重规则（艾宾浩斯衰减机制）**：
- 题目初次答错加入错题本，初始权重为 **3**。
- 再次答错：权重 **+1**，上限为 **8**（防止恶意刷错导致权重无意义膨胀）。
- 答对一次：权重 **-1**。
- 权重降至 **0**：移出错题本，回归普通题池参与 C 题抽取。